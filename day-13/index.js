/*
let contacts = [
  { id: 1, name: "bani", phone: "123" },
  { id: 2, name: "aisa", phone: "456" },
  { id: 3, name: "wahyu", phone: "789" },
];

function editContact(contacts, id, object) {
  const updateContacts = contacts.map((item) => {
    if (item.id === id) {
      return { ...item, ...object };
    }
    return item;
  });
  return updateContacts;
}

const editBani = editContact(contacts, 1, { phone: "6666" });
console.log(editBani);


// setInterval(function () {
//   console.log("Hello");
// }, wqdfuq);

const insertName = (name) => {
  console.log(`my name is ${name}`);
};

insertName("burhanudin Rabbani");

const logName = (name) => {
  console.log(`My name is ${name}`);
};

const insertName = (callback) => {
  const name = "Alpaca";
  callback(name);
};

insertName(logName);*/
